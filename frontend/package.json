{
  "name": "interactive-textbook-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:admin",
    "build:admin": "esbuild src/admin-render.js --bundle --format=iife --minify --outfile=dist/admin-render.js",
    "preview": "vite preview",
    "test": "vitest run",
    "mock-server": "node mock-server/server.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "jsdom": "^24.1.3",
    "svelte": "^4.2.19",
    "vite": "^5.4.11",
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "@fontsource/eb-garamond": "^5.2.7",
    "temml": "^0.11.11"
  }
}
